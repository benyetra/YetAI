# Core Framework
fastapi==0.115.6
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
email-validator==2.1.0

# Database
psycopg2-binary==2.9.9
sqlalchemy==2.0.23
alembic==1.13.1

# API Clients
httpx==0.25.2
aiohttp==3.12.14
requests==2.32.4

# Sports Stats APIs (for player prop verification)
# nfl-data-py metadata pins numpy<2; install with --no-deps (see Dockerfile). Local:
#   cd backend && .venv/bin/pip install nfl-data-py==0.3.3 --no-deps && .venv/bin/pip install appdirs fastparquet
nba-api==1.11.4
MLB-StatsAPI==1.9.0
pybaseball>=2.2.7
pyarrow>=17.0.0
matplotlib>=3.7.0,<4.0
lxml>=4.9.0
scipy>=1.11.0
sportsdataverse>=0.0.50
joblib>=1.3.0
s3fs>=2024.2.0

# AI/ML
openai==1.3.9
scikit-learn==1.5.0
# HR ML model (joblib) and sklearn 1.5 wheels expect numpy 2.x (numpy._core)
pandas==2.2.3
numpy==2.1.3
pillow==10.4.0

# Background Tasks
celery==5.3.4
redis==5.0.1

# Authentication & Security
pyjwt[crypto]==2.10.1
passlib[bcrypt]==1.7.4
python-multipart==0.0.18
pyotp==2.9.0
PyYAML==6.0.3
qrcode==7.4.2
google-auth==2.30.0
google-auth-oauthlib==1.2.0

# Payment Processing
stripe==11.2.0

# Notifications
twilio==9.1.0

# File Storage
boto3==1.35.0
botocore>=1.35.0

# ML inference (XGBoost points model — Development-flm)
xgboost[cpu]>=2.0.0

# Environment Management
python-dotenv==1.0.0

# Injury scrape (CBS Sports HTML) — Development-1nt
# (nba-api==1.11.4 already pinned above; do not re-add as nba_api — pip errors on conflict.)
beautifulsoup4==4.12.3

# Development
pytest==7.4.3
pytest-asyncio==0.21.1
black==24.3.0
flake8==6.1.0
